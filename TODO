v1.0
	kill all threads when closing sonata?
	ability to 'save all' for tag editing? shortcut?
	ability to switch between songs in playlist for info window?
	post drag selection..
	artist/album view, everything selected, add/replace
	search -> edit tag

Future:
	make selection, dnd, more like thunar
	connect in background via socket.setblocking(0)
	last.fm?
