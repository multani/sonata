v1.0
	make selection, dnd, more like thunar
	lyrics (lyricwiki slow; closing sonata during thread; mult threads)
	ability to 'save all' for tag editing
	python 2.5
	make editwindow labels grey..

Future:
	connect in background via socket.setblocking(0)
	last.fm?
