v1.0
	make selection, dnd, more like thunar
	add actions to infowindow art..
	lyrics
	ability to 'save all' for tag editing
	python 2.5
	selection not retained in artist view level 2

Future:
	USE COMMAND_LIST_* for batch processes (i.e. remove, crop, add_*)
	connect in background via socket.setblocking(0)
	last.fm?
